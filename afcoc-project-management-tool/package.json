{
  "name": "afcoc-project-management-tool",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "libs/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.0.0"
  }
}